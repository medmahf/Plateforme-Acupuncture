<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Inscription – Plateforme Acupuncture</title>
    <link rel="stylesheet" href="/site/style.css">
</head>
<body>
<div class="background-image">
    <header>
        <nav role="navigation" aria-label="Menu principal">
            <a href="../index.php">Accueil</a>
            <a href="../index.php?page=patho">Pathologies</a>
            <a href="../index.php?page=contact">Contact</a>
            <span class="nav-status">
            {% if session.username is defined %}
                <span class="nav-user">Bienvenue, {{ session.username }}</span> |
                <a href="../index.php?page=logout">Déconnexion</a>
            {% else %}
                <a href="../index.php?page=login">Connexion</a> |
                <a href="../index.php?page=signup">Inscription</a>
            {% endif %}
            </span>
        </nav>
    </header>
    <main class="container">
        <h1>Créer un compte</h1>
        {% if erreur %}
            <div class="alert alert-danger" style="color: red;" role="alert" aria-live="assertive">{{ erreur }}</div>
        {% endif %}
        {% if succes %}
            <div class="alert alert-success" style="color: green;" role="alert" aria-live="polite">{{ succes }}</div>
        {% endif %}
        <form action="../index.php?page=signup" method="post" aria-label="Formulaire d'inscription">
            <label for="new-username">Nom d'utilisateur :</label>
            <input type="text" id="new-username" name="username" required autocomplete="username">
            <label for="email">Adresse e-mail :</label>
            <input type="email" id="email" name="email" required autocomplete="email">
            <label for="new-password">Mot de passe :</label>
            <input type="password" id="new-password" name="password" required autocomplete="new-password">
            <input type="submit" value="Créer le compte">
        </form>
        <p>Déjà un compte ? <a href="../index.php?page=login">Se connecter</a></p>
    </main>
    <footer>
        © 2025 Plateforme d’acupuncture
    </footer>
</div>
</body>
</html>
